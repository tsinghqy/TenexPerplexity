# Known Limitations

- P0 scaffold has no auth, chat, or search yet — those arrive in later phases.
- Media generation mode from GraphChat is intentionally out of scope.
- Embedding DB cache migration may be deferred; in-memory LRU is acceptable initially.
